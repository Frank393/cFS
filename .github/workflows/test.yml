name: ACT_Test

# Run every time a new commit pushed or for pull requests
on:
  push:
  pull_request:

env:
  ACT: false
jobs:
  ACT_job:
    if: ${{ env.ACT == 'false' }}
    runs-on: ubuntu-latest
    steps:
      - name: "Hello world"
        run: echo "ACT worked"
    
